import { makeStyles } from "@material-ui/core"

export default makeStyles(() => ({
    form: {
        backgroundColor: 'white',
        padding: 16,
    },

    title: {
        fontSize: 20,
        marginBottom: 8,
    },

    helperText: {
        height: 19,
        marginBottom: 16
    },

    btn: {
        width: '100%',
    }
}))