import { makeStyles } from "@material-ui/core"

export default makeStyles(() => ({
    root: {
        width: '100%',
        marginBottom: 16,
    },
    
    categories: {
        flexDirection: 'row',
        justifyContent: 'space-between'
    },
}))