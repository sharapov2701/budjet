import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
    root: {
        width: '75%',
    },

    list: {
        backgroundColor: 'white',
        marginTop: 16,
    }
}))